#!/bin/bash

echo "BackupScript Has Started!"

mkdir -p backup
cd backup

currentDate=$(date -d today '+%Y-%m-%d')
mkdir -p $currentDate
cd ..

for file in $(ls *.txt); do
cp -n $file ./backup/$currentDate
echo $file "Has been backed up into" $currentDate
done

