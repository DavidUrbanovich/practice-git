#!/bin/bash

echo "BackupScript Has Started!"

mkdir -p backup
cd backup

filename="Backup#$(ls -l | grep '^d' | wc -l)"
mkdir -p $filename
cd ..

for file in $(ls *.txt) 
do
	cp -n $file ./backup/$filename
	echo $file "Has been backed up into" $filename
done
